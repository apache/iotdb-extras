apiVersion: v1
kind: PersistentVolume
metadata:
  name: datanode-storage2
  labels:
    app: datanode-storage
spec:
  capacity:
    storage: 1Gi
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  hostPath:
    path: /data/k8s_iotdb/data/datanode2
    type: DirectoryOrCreate